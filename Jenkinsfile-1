pipeline {

  agent any
  stages {

    stage('Checkout Source') {
      steps {
        git url:'https://github.com/myorg009/acme_fitness_demo.git', branch:'master'
      }
    }

    stage('Deploy App') {
      steps {
      script {
          sh 'sudo /usr/bin/kubectl config use-context aws-workload-04-tanzu-cluster-admin@aws-workload-04-tanzu-cluster --kubeconfig=/var/aws-config/admin.conf'
          sh 'sudo /usr/bin/kubectl create ns shopping'
          sh 'sudo /usr/bin/kubectl get ns'
         // sh 'sudo /usr/local/bin/kubectl apply -f kubernetes-manifests/secrets.yaml -n shopping'
         // sh 'sudo /usr/local/bin/kubectl apply -f kubernetes-manifests/acme_fitness_cluster1.yaml -n shopping'
         // sh 'sudo /usr/local/bin/kubectl apply -f gateway.yaml -n shopping'
}
      }
    }

  }

}
